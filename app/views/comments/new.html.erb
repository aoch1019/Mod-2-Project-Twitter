<%= form_for [@user, @tweet, @comment] do |f| %>

  <%= f.hidden_field :user_id, value: params[:user_id] %>
  <%= f.hidden_field :tweet_id, value: params[:tweet_id] %>
  
  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.submit %>

<% end %>
